
# Path to master subtree branch directory
polaris_master : '/gpfs/fs1/home/sbrus/polaris_worktrees/main'

# Name of subtree branch to create
branch_name : 'cosine_bell_viz'

# Remote compass branch to checkout (assumes remote has been added previously)
remote_branch : 'xylar/make-cosine-bell-viz-off-by-default'

# Master compass branch to use for local merge
master_branch : 'origin/main'

# Path to miniconda installation
miniconda_path : '/gpfs/fs1/home/sbrus/mambaforge'

# MPI implementation to use for conda package install
mpi : 'impi'

# Compiler to use for conda package install
compiler : 'intel'

# Make target for MPAS-O compilation
make_target : 'intel-mpi' 

# Option for using debug flags for MPAS-O compilation
debug : False

# List of testcases to setup
testcases : ['ocean/global_convergence/qu/cosine_bell','ocean/global_convergence/qu/cosine_bell_with_viz','ocean/global_convergence/icos/cosine_bell','ocean/global_convergence/icos/cosine_bell_with_viz']

# Work directory to use for test case setup
workdir : '/lcrc/group/acme/sbrus/polaris_cosine_bell_viz'

# Option to update the subtree branch with the remote branch HEAD
update_branch : True 

# Option to perform local merge to master compass branch
local_merge : True

# Option to configure the conda environment
configure_conda : False

# Option to compile MPAS-O
compile_mpas : False

# Option to setup list of testcases
setup_testcases : True

# Option to submit test case jobs
run : True

# Remote to use for E3SM checkout (optional)
e3sm_remote : ''

# Branch to use for E3SM checkout (optional)
e3sm_branch : ''

# Commit to use for E3SM checkout (optional)
e3sm_commit : ''
